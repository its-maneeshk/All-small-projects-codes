bool binarySearch(int arr, int n, int key) {
    int start = 0;
    int end = n - 1;

    for(int i=0; i<n; i++) {
        if(start <= end) {
            int mid = start + end / 2;
        }  
    }
}